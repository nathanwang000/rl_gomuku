[project]
name = "gomoku"
version = "0.1.0"
description = "A clean, extensible Gomoku (Five in a Row) game designed as a playground for RL experiments."
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
    "numpy>=1.24",
]

[project.scripts]
gomoku-server = "app:main"
