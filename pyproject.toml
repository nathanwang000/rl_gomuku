[project]
name = "gomoku"
version = "0.2.0"
description = "Gomoku (Five in a Row) — serverless-ready with client-side game state and stateless AI endpoint."
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
]

[project.scripts]
gomoku-server = "app:main"
